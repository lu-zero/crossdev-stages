# Boards whose CPU has no V extension.  Building with it on produces a kernel
# the silicon cannot run, and the failure is a trap at first use, not a
# link error, so it has to be asserted rather than assumed.
# CONFIG_RISCV_ISA_V is not set
# CONFIG_RISCV_ISA_V_DEFAULT_ENABLE is not set
