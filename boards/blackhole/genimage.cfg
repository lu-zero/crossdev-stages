config {
    outputpath = .
    inputpath = .
    rootpath = gen
    tmppath = tmp
}

image gentoo-linux-blackhole-rootfs.ext4 {
    ext4 {
        use-mke2fs = "true"
        label = "rootfs"
    }

    size = 10G
    mountpoint = "/root"
}
