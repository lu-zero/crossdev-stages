# Vendor UART on VSRVES01 — Xilinx UARTLite, fixed 1 Mbaud (configured
# in the FPGA build).  /etc/init.d/agetty.ttyUL0 is a symlink to
# /etc/init.d/agetty (openrc template), enabled in default runlevel by
# override-kernel.sh after target install.
baud="1000000"
term_type="vt100"
