# Host-side tools needed by the board's override scripts.
# mksquashfs is invoked by override-kernel.sh stage 3 to pack /target
# into image.sqfs, the phram-mounted rootfs payload.
sys-fs/squashfs-tools
# unzip is invoked by override-checkout.sh to unpack the vendor
# vsrv_root_<DATE>.zip (not in stage3 @system).
app-arch/unzip
