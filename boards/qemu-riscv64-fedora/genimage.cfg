config {
    outputpath = .
    inputpath = .
    rootpath = gen
    tmppath = tmp
}

image fedora-linux-qemu-riscv64-rootfs.ext4 {
    ext4 {
        use-mke2fs = "true"
        label = "rootfs"
    }

    size = 3G
    mountpoint = "/root"
}
