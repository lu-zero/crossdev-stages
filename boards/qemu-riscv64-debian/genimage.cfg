config {
    outputpath = .
    inputpath = .
    rootpath = gen
    tmppath = tmp
}

image debian-linux-qemu-riscv64-rootfs.ext4 {
    ext4 {
        use-mke2fs = "true"
        label = "rootfs"
    }

    size = 4G
    mountpoint = "/root"
}
