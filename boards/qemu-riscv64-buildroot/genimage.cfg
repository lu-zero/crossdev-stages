config {
    outputpath = .
    inputpath = .
    rootpath = gen
    tmppath = tmp
}

image buildroot-linux-qemu-riscv64-rootfs.ext4 {
    ext4 {
        use-mke2fs = "true"
        label = "rootfs"
    }

    size = 1G
    mountpoint = "/root"
}
